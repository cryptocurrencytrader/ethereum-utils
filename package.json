{
  "name": "@bitcointrade/ethereum-utils",
  "version": "1.0.1",
  "description": "Utilities to handle Ethereum operations",
  "repository": "git@github.com:cryptocurrencytrader/ethereum-utils.git",
  "author": "Rafael Tavares <rafael@bitcointrade.com.br>",
  "license": "LGPL-3.0",
  "private": false,
  "scripts": {
    "run-publish": "tslint -c tslint.json 'src/**/*.ts' && rimraf .publishing && tsc --outDir .publishing && copy package.json .publishing && copy README.md .publishing && npm publish .publishing"
  },
  "dependencies": {
    "@types/crypto-js": "^3.1.39",
    "crypto-js": "^3.1.9-1",
    "tslib": "^1.9.0"
  },
  "devDependencies": {
    "copy": "^0.3.1",
    "rimraf": "^2.6.2",
    "tslint": "^5.9.1",
    "typescript": "^2.8.3"
  }
}
